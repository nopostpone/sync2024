#include <bits/stdc++.h>
using namespace std;

using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;
using u128 = unsigned __int128;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    freopen("x.in", "r", stdout);

    mt19937_64 rng{chrono::steady_clock::now().time_since_epoch().count()};

    

    return 0;
}