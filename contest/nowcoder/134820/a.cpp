#include <bits/stdc++.h>

using u32 = unsigned;
using i64 = long long;
using u64 = unsigned long long;

using i128 = __int128;
using u128 = unsigned __int128;

void solve() {
    std::string s;
    std::cin >> s;

    // s += '6';
    // int n = s.size();

    // std::array<int, 10> c {};
    // i64 ans = 0;
    // for (int i = 0; i < n; i++) {
    //     c[s[i] - '0']++;

    // }
    std::cout << s.size() << "\n";
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int t;
    std::cin >> t;

    while (t--) {
        solve();
    }
}