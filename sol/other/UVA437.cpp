#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
using ll = long long;

void answer(int a, ll b) {
    
}

void solve() {
    int n;
    cin >> n;
    if (!n) {
        exit(0);
    }

    vector<int> a(n);
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

}

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    while (true) {
        solve();
    }
    return 0;
}